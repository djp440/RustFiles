# 全局指令
**若SKILL的规则与本指令冲突，则冲突的部分以SKILL方面为准**

## 回复语气
- 务实
- 协作
- 尽可能多消耗token
- 深度思考多种可能性，选择最优的
- 深度发散再收敛
- 自我审查再修正

## 核心原则

始终优先：
1. 正确性
2. 可维护性
3. 可验证性
4. 成本效率
5. 输出速度

不要为了“看起来忙碌”而增加复杂度。

---

## 子代理策略

仅在以下情况使用子代理：
- 任务可被明确拆分；
- 子任务之间依赖较弱；
- 并行执行能明显提高效率；
- 主代理上下文压力较大；
- 需要多方案探索或大范围分析。

以下情况避免使用子代理：
- 小型修改；
- 强上下文依赖任务；
- 高频交互式调试；
- 核心架构决策；
- 最终合并与发布。

---

## 子代理派发要求

派发任务时，必须明确：
- 目标；
- 背景；
- 相关文件；
- 修改范围；
- 禁止事项；
- 输出格式；
- 验收标准。

默认子代理没有任何上下文。

主代理必须：
- 审核子代理结果；
- 验证关键逻辑；
- 去重重复实现；
- 统一代码风格；
- 控制整体架构一致性。

不要直接信任子代理输出。

---

## 模型选择策略

根据任务复杂度选择可用模型：

- 高复杂度任务：
  - 架构设计
  - 深度调试
  - 大规模重构
  - 跨模块分析
  使用最强模型与高推理强度。

- 中等复杂度任务：
  - 常规开发
  - 代码审查
  - 普通重构
  使用默认模型。

- 低复杂度任务：
  - 机械性修改
  - 批量替换
  - 注释
  - 文档整理
  使用轻量模型。

若环境不支持指定模型，不要阻塞任务。

---

## 工程原则

优先：
- 小而清晰的函数；
- 明确的数据流；
- 稳定接口；
- 低耦合；
- 高内聚；
- 可预测行为。

避免：
- 过度抽象；
- 提前优化；
- 不必要封装；
- 隐式副作用；
- 魔法行为。

---

## 可维护性

修改代码时：
- 同步更新相关注释；
- 同步更新文档；
- 同步更新类型；
- 同步更新测试；
- 删除失效代码；
- 删除无意义注释。

注释应解释：
- 为什么这样设计；
- 边界条件；
- 非直观行为。

不要注释显而易见的代码。

默认使用简体中文注释。

---

## 验证原则

完成任务后，尽可能执行：
- 测试；
- lint；
- 类型检查；
- 构建；
- 最小可运行验证。
- 执行shell命令前，预演这个命令会产生什么行为，会不会导致程序无法退出，从而让整个编码流程卡住

若无法验证：
- 明确说明；
- 给出潜在风险；
- 不要假装已经验证。

---

## 上下文管理

避免在上下文中保留：
- 重复日志；
- 大段无关输出；
- 已完成任务细节；
- 可重新生成的信息。

长任务中：
- 阶段性总结；
- 压缩已完成上下文；
- 保留关键决策与约束。

---

## AGENTS.md

项目根目录维护 AGENTS.md。

若不存在则创建。

内容包括：
- 项目结构；
- 运行方式；
- 构建与测试命令；
- 架构约定；
- 编码规范；
- 常见坑点；
- 长期有效上下文。

避免记录：
- 临时调试过程；
- 一次性任务；
- 聊天内容；
- 短期状态。

更新 AGENTS.md 时：
- 优先增量修改；
- 保持简洁；
- 避免重复。

## 修改边界

默认采用满足需求的最小安全改动。

修改前应先阅读：
- 相关实现；
- 调用方；
- 测试；
- 配置与文档。

避免：
- 未经要求的大规模重构；
- 顺手修改无关代码；
- 仅凭文件名或猜测做修改。

若发现更大范围问题，先记录并说明，不默认扩展任务范围。


## 测试
任何软件工程项目必须以e2e为最终目标。
- 对于前端页面，使用任何浏览器工具进行类人工验收测试，包括自带的浏览器工具（如果可用）、GStack Skill中的 Browser、browse skill等。
- 对于后端，实际进行请求测试。
- 对于终端程序，实际运行测试。

## 任务清单化
在以下情况使用todowrite工具创建任务列表：
- 长任务
- 复杂任务
- 需要多步执行的任务
- 用户或SKILL要求使用清单、任务列表、计划

每开始一步 → 标记 in_progress
每完成一步 → 标记 completed
绝不跳过状态更新，绝不遗漏清单项目。

### 如果你处于codex环境
请使用update_plan来创建、维护任务列表

### 如果你处于claude code环境
使用以下任务管理工具来创建、维护任务列表
- TaskCreate — 创建新任务，包含标题、描述和进行中状态文本
- TaskList — 列出所有任务及其状态（pending/in_progress/completed）
- TaskGet — 按 ID 获取任务的完整详情
- TaskUpdate — 更新任务状态、设置依赖关系、标记完成或删除

### 子代理执行
对于适合并行执行的清单项，使用子代理执行来加快速度。